<UserControl x:Class="Functions.Views.FunctionPointsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:cal="http://www.caliburnproject.org">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Grid.Row="0"
                    Height="30">
            <TextBlock Text="Абсолютная отметка [мм]"
                       VerticalAlignment="Center"/>
            <TextBlock Text="Температура [°C]"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding FunctionPoints}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding X}"
                                     Height="25"
                                     VerticalContentAlignment="Center"
                                     Grid.Column="0"
                                     Margin="3"/>
                            <TextBox Text="{Binding X}"
                                     Height="25"
                                     VerticalContentAlignment="Center"
                                     Grid.Column="1"
                                     Margin="3"/>
                            <Button  Width="25"
                                     Height="25"
                                     Grid.Column="2">
                                <Button.Content>
                                    <Path Data="M0,0 L1,1 M0,1 L1,0" 
                                          Stroke="Black"
                                          StrokeThickness="1"
                                          Stretch="Uniform"
                                          Width="8" Height="8"/>
                                </Button.Content>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Content="Добавить точку"
                cal:Message.Attach="CreateNewPoint"
                Grid.Row="2"
                HorizontalAlignment="Center"
                Margin="10"
                Width="150"
                Height="30" />
    </Grid>
</UserControl>
